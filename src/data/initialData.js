import db from './db.json';

export const initialProjects = db.projects;
export const initialSuppliers = db.suppliers;
export const initialMaterials = db.materials;
export const initialExpenses = db.expenses;
export const initialSchedule = db.schedule;
export const initialEmployees = db.employees;
export const initialInventoryMovements = db.inventoryMovements;
export const initialHistory = db.history;

export default db;
